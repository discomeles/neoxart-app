import NoteCard from './NoteCard'
import AppNavbar from './AppNavbar'

const HomePage = ({info}) => {
    return (
        <>
        <NoteCard info={info}/>
        </>
    )
}

export default HomePage